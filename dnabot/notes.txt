ok thinking about it I think what I would like to do rather than doing the clustering approach if clip number > 96 is instead to start by assigning assemblies to assembly plates (according to whether keep_layout is true or not, if true then no change required, if false then consolidate by removing gaps and then split into blocks of up to 96), then starting from the first and the last assembly, find out how many clips for each set and then if less than 96 clips try again with the next assembly plate too (i.e. first and second for the forwards direction or last and second last for the backwards direction. continue until either the i+1 step will be over 96 or all assembly plates are satisfied. This should split the assembly plates between one clip plate or the other. If there is one assembly plate left in the middle then try adding assemblies in sets of 10 to the forwards and backwards direction, if this leaves 10 in the middle then go through on a one by one basis, if this fails then use the original clip mapping. I want to make sure that when the requirements for this mapping are satisfied that the new clip mapping is used to generate the clip and mag purification scripts and is also used for the metadata